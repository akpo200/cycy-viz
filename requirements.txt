# ============================================================
# requirements.txt — Dépendances du projet Sénégal Analytics
# ============================================================
# Installation : pip install -r requirements.txt

# --- Framework Web ---
Flask==3.1.2
dash==3.3.0
dash-bootstrap-components==2.0.4
gunicorn==23.0.0

# --- Visualisation ---
plotly==6.5.0
matplotlib==3.8.4

# --- Données ---
pandas==2.2.2
openpyxl==3.1.2          # Lecture fichiers Excel .xlsx

# --- Base de données ---
pymongo==4.16.0           # Client MongoDB (optionnel — fallback fichier si absent)

# --- Extraction PDF & OCR ---
pdfplumber==0.11.9        # Extraction de tableaux depuis PDF
fpdf2==2.8.7              # Génération de rapports PDF
pytesseract==0.3.13       # OCR (optionnel — nécessite Tesseract installé)
Pillow==11.2.1            # Traitement images (requis par pytesseract)

# --- Web Scraping ---
requests==2.32.5
beautifulsoup4==4.14.3

# --- Tests ---
pytest==8.3.5
